const INITIAL_STATE = {
    itemList: [],
};

const rootReducer= ( state = INITIAL_STATE , action)=>{

   /* switch (action.type) {
        case value:
            
            break;
    
        default:
            break;
    }*/
}

export default rootReducer;